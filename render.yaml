# DISABLED. The current application cannot safely run as a split Render
# service because cross-process records and artifacts are not canonical.
# Follow docs/release/RUNBOOK.md; do not add services until that gate closes.
services: []
